---
title: "BAE/<JOON 1969"
date: 2013-03-01 08:26:28 -0400
categories: algo
---
---

> DNA [GO 1969]

### CODE
```java
class Main {
	public static void main(String[] args) throws Exception {
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		StringBuilder sb = new StringBuilder();
		StringTokenizer st = new StringTokenizer(br.readLine());

		int n = Integer.parseInt(st.nextToken());
		int m = Integer.parseInt(st.nextToken());
		String[] dna = new String[m];
		for (int i = 0; i < n; i++)
			dna[i] = br.readLine();

		int a, c, g, t, max, d = 0;
		for (int i = 0; i < m; i++) {
			a = c = g = t = max = 0;
			for (int j = 0; j < n; j++)
				switch (dna[j].charAt(i)) {
					case 'A': a++; break;
					case 'C': c++; break;
					case 'G': g++; break;
					case 'T': t++; break;
				}
			max = Math.max(a > c ? a : c, g > t ? g : t);
			d += n - max;
			if (a == max) sb.append('A');
			else if (c == max) sb.append('C');
			else if (g == max) sb.append('G');
			else if (t == max) sb.append('T');
		}
		sb.append("\n" + d);
		System.out.print(sb);
	}
}
```

### COMMENT
* Greed


[GO 1969]: https://www.acmicpc.net/problem/1969
